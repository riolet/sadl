# Web stack library - common node classes for web applications

#nodeclass
browser::
    http_connector

web_server::
    http_listener (80)
    https_listener (443)
    db_connector

database::
    db_listener (5432)

cache::
    cache_listener (6379)

#linkclass
browser.http_connector -> web_server.http_listener
browser.http_connector -> web_server.https_listener
web_server.db_connector -> database.db_listener
web_server.db_connector -> cache.cache_listener
